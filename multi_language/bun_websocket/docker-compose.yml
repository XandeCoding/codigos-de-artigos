services:
  dragonfly:
    image: "docker.dragonflydb.io/dragonflydb/dragonfly"
    ulimits:
      memlock: -1
    ports:
      - "6379:6379"
    volumes:
      - dragonflydata:/data
    command: ["--maxmemory", "256mb", "--proactor_threads", "1"]
volumes:
  dragonflydata:
